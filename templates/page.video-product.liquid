<div class="page-width">
  <section id="NovBreadcrumbs" class="text-center">
    <div class="container">
      <h1 class="headingPage text-uppercase">{{ page.title }}</h1>
    </div>
  </section>
  <div class="container">
    {% if page.content.size > 0 %}
      <div class="rte">
        {{ page.content }}
      </div>
    {% endif %}
  </div>
</div>
